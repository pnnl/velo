package gov.pnnl.cat.core.resources;


public interface IRemoteFileManager {

  // need a method to get all registered remote endpoints
  
  // need a method to create a connection to a given remote endpoint - this should wrap authentication
  
  // need methods to interact with a remote endpoint
  
  
}
