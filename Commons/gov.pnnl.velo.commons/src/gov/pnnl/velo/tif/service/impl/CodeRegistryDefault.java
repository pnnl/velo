package gov.pnnl.velo.tif.service.impl;

import org.apache.log4j.Logger;

/**
 * Load codes just like we load machines
 *
 */
public class CodeRegistryDefault extends CodeRegistryAdapter {
  private static Logger logger = Logger.getLogger(CodeRegistryDefault.class);

}
